services:
  api:
    build: .
    restart: always
    expose:
      - "8000"
    ports:
      - "127.0.0.1:8000:8000"
    environment:
      - DATABASE_URL=postgresql+asyncpg://imo_user:${POSTGRES_PASSWORD}@db:5432/imo_db
      - JWT_SECRET=${JWT_SECRET}
      - JWT_REFRESH_SECRET=${JWT_REFRESH_SECRET}
      - CORS_ORIGINS=${CORS_ORIGINS:-*}
      - ACCESS_TOKEN_EXPIRE_MINUTES=${ACCESS_TOKEN_EXPIRE_MINUTES:-30}
      - REFRESH_TOKEN_EXPIRE_DAYS=${REFRESH_TOKEN_EXPIRE_DAYS:-14}
      - REDIS_URL=redis://redis:6379/0
      # Google Gemini API — 운동 챗봇 (Phase C). 시연 직전 LLM_MODEL=gemini-2.5-pro 토글.
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - LLM_MODEL=${LLM_MODEL:-gemini-2.5-flash}
      - CHAT_HISTORY_TTL=${CHAT_HISTORY_TTL:-3600}
      - CHAT_MAX_TURNS=${CHAT_MAX_TURNS:-10}
    depends_on:
      - db
      - redis
    networks:
      - imo_net

  db:
    image: postgres:15-alpine
    restart: always
    environment:
      POSTGRES_USER: imo_user
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: imo_db
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - imo_net

  # Redis 캐시 — 외부 미노출, imo_net 내부 통신만
  redis:
    image: redis:7-alpine
    restart: always
    volumes:
      - redis_data:/data
    networks:
      - imo_net

volumes:
  postgres_data:
  redis_data:

networks:
  imo_net:
